// User function Template for Java
class Solution {
    static boolean armstrongNumber(int n) {
        // code here
        long sum=0,rem,temp=n;
        while(n>0){
          rem=n%10;
          sum+=(rem*rem*rem);
          n/=10;
        }
        return sum==temp;
    }
}
